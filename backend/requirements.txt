# Reproducible Windows installation uses requirements.lock.txt through
# scripts/setup-windows.ps1. Do not install this file directly: InsightFace
# declares CPU onnxruntime, which conflicts with intended GPU package.
-r requirements.lock.txt
